spring:
  application:
    name: auth-service
  config:
    import: nacos:auth-service-dev.yml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
        refresh-enabled: true
        auto-refresh: true
        enable-remote-sync-config: true

